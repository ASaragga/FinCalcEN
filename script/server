#Quick and Dirty Jekyll/Compass setup by @mattgraham
#!/bin/sh
#
# Usage:
#   server
#   open http://localhost:4000

set -e
cd $(dirname "$0")/..

bundle install
script/bootstrap && bundle exec foreman start && bundle exec middleman build
exit=$?
exit $exit
